<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>New question</title>
</head>

<body style="background-color: rgb(182, 247, 217);;">
    <div class="grid grid-cols-6">
        <div class="col-start-2 col-span-4">
            <span class="text-2xl font-dark ">Add Question</span>
          <form action="/createquestions/<%= id %>" method="post">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <div class="py-3">
           <label for="questionname" class="block mt-3 font-semibold">Enter Question:</label>
           <input id="questionname" name="questionname" type="text" placeholder="Question" class="border border-gray-300 text-gray-900 rounded p-2 w-full">
        </div>  
        <div class="py-3"> 
           <label for="description">Description:</label>
           <input id="description" name="description" type="text" placeholder="Description" class="border border-gray-300 text-gray-900 rounded p-2 w-full">
        </div>  
        <div class="py-3"> 
           <button type="submit" class="text-white bg-green-600 hover:bg-green-800 font-medium rounded px-5 py-2">Create</button>        
        </div>  
        </form>
        <% if(messages && messages.error && messages.error.length > 0)  { %>
            <% for(var i=0; i<messages.error.length; i++) { %>
              <p class="bg-red-200 my-3 list-none rounded px-4 py-2"><%= messages.error[i] %></p>
            <% } %>
          <% } %>
        </div>
    </div>
  </body>
</html>